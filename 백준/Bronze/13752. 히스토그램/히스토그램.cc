#include <bits/stdc++.h>
#define endl "\n"
#define io ios_base::sync_with_stdio(false);cin.tie(nullptr);
using namespace std;

int main(){
    int t;
    int n;
    cin>>t;
    while(t--){
        cin>>n;
        for(int i=0; i<n; i++){
            cout<<"=";
        }
        cout<<endl;
    }
}