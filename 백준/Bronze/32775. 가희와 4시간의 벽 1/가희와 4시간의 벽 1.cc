#include <bits/stdc++.h>
using namespace std;

int main() {
    int s, f;
    cin >> s >> f;

    if (s > f) {
        cout << "flight";
    } else {
        cout << "high speed rail";
    }

    return 0;
}